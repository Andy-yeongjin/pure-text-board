/**
 * common/server.js와의 통합을 위한 헬퍼 유틸리티
 */

export const startSharedServerProxy = async () => {
  console.log('Shared server proxy initialized for Port 3004');
  // 실제 common/server.js의 startSharedServer 호출 로직이 들어갈 자리
};
